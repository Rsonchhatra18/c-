#include<iostream>
using namespace std;
int main()
{ /*     increasing order
    int arr[100];
    int n;
    cout<<"Enter number of terms :";
    cin>>n;
    cout<<"Enter array elements : \n";
    for(int i=0;i<n;i++)
    {
        cout<<"Element"<<" "<<i+1<<" :";
        cin>>arr[i];
    }
    for(int i=n-2;i>=0;i--)
    {
        bool swapped=0;
        for(int j=0;j<=i;j++)
        {
            if(arr[j]>arr[j+1])
            {
            swap(arr[j],arr[j+1]);
            swapped=1;
            }
        }
        if(swapped==0)
        break;
    }
    cout<<"Sorted array is :\n";
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    } */ 
    //decreasing order
   /* int arr[100];
    int n;
    cout<<"Enter number of terms :";
    cin>>n;
    cout<<"Enter array elements : \n";
    for(int i=0;i<n;i++)
    {
        cout<<"Element"<<" "<<i+1<<" :";
        cin>>arr[i];
    }
    for(int i=n-2;i>=0;i--)
    {
        bool swapped=0;
        for(int j=0;j<=i;j++)
        {
            if(arr[j]<arr[j+1])
            {
            swap(arr[j],arr[j+1]);
            swapped=1;
            }
        }
        if(swapped==0)
        break;
    }
    cout<<"Sorted array is :\n";
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    } */
}