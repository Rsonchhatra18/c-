#include<iostream>
using namespace std;
int main()
{ /*    //ascending order
    int arr[100],n;
    cout<<"Enter number of terms :";
    cin>>n;
    for(int i=0;i<n;i++)
    {
        cout<<"Enter array element"<<" "<<i +1<<":";
        cin>>arr[i];
    }
    for(int i=0;i<n-1;i++)
    {
        int index=i;
        for(int j=i+1;j<n;j++)
    {
        if(arr[j]<arr[index])
        index=j;
    }
    swap(arr[index],arr[i]);
}
    cout<<"Sorted array:\n";
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    }*/
   /*   descending order
   int arr[100],n;
    cout<<"Enter number of terms :";
    cin>>n;
    for(int i=0;i<n;i++)
    {
        cout<<"Enter array element"<<" "<<i +1<<":";
        cin>>arr[i];
    }
    for(int i=0;i<n-1;i++)
    {
        int index=i;
        for(int j=i+1;j<n;j++)
    {
        if(arr[j]>arr[index])
        index=j;
    }
    swap(arr[index],arr[i]);
}
    cout<<"Sorted array:\n";
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    }*/
}