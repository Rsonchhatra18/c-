#include<iostream>
using namespace std;
int main()
{   /*  increasing order
    int arr[100],n;
    cout<<"Enter number of terms :";
    cin>>n;
    cout<<"Enter array elemnts :\n";
    for(int i=0;i<n;i++)
    {
        cout<<"Element "<<i+1<<" :";
        cin>>arr[i];
    }
    for(int i=1;i<n;i++)
    {
        for(int j=i;j>0;j--)
        {
            if(arr[j]<arr[j-1])
            swap(arr[j],arr[j-1]);
            else
            break;
        }
    }
    cout<<"Sorted array :\n";
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    }*/
   /*   decreasing order  
    int arr[100],n;
    cout<<"Enter number of terms :";
    cin>>n;
    cout<<"Enter array elemnts :\n";
    for(int i=0;i<n;i++)
    {
        cout<<"Element "<<i+1<<" :";
        cin>>arr[i];
    }
    for(int i=1;i<n;i++)
    {
        for(int j=i;j>0;j--)
        {
            if(arr[j]>arr[j-1])
            swap(arr[j],arr[j-1]);
            else
            break;
        }
    }
    cout<<"Sorted array :\n";
    for(int i=0;i<n;i++)
    {
        cout<<arr[i]<<" ";
    }*/
}